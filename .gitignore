node_modules/
dist/
coverage/
*.tgz
*.log

# Never commit real credentials. Ship .env.example instead.
.env
.env.*
!.env.example

# `npm publish` and actions/setup-node both write a registry auth token into a
# local .npmrc. It must never be committed.
.npmrc

.DS_Store
.idea/
.vscode/
.scratch/
